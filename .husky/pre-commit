STAGED=$(git diff --cached --name-only)
[ -n "$STAGED" ] && echo "$STAGED" | xargs npx prettier --write --ignore-unknown 1>/dev/null 2>&1
echo "$STAGED" | xargs git add
npm run typecheck && npm run lint && npm run duplicate && npm test